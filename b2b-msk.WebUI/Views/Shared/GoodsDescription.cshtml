@model b2b_msk.Domain.Entities.Good
    <div class="well">
        <h3>
            <strong>@Model.Name</strong>
            <span class="pull-right label label-primary">@Model.curPrice.ToString("# руб")</span>
        </h3>
        <span class="lead">@Model.Description</span>
    </div>
